name: Test

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  check-i386:
    name: 32-bit container check for ${{ matrix.bin }}
    runs-on: ubuntu-latest
    container:
      image: i386/debian:12
    strategy:
      fail-fast: false
      matrix:
        include:
          - {bin: form}
    steps:
      - run: |
          dpkg --add-architecture amd64
          apt-get update
          apt-get install --no-install-recommends -y libstdc++6:amd64

      # See https://github.com/actions/checkout/issues/334
      - name: Checkout repository
        uses: actions/checkout@v6

      - run: ls
